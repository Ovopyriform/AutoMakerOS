--name ${project.name}
--app-image "${project.build.directory}/${project.name}.app"
--app-version ${project.version}
--temp "${project.build.directory}/installer-work"
${macos.sign}
${macos.sign.identity}